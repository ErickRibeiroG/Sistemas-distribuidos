Você deverá criar a classe Empresa (String cnpj, String nome, int numEmpregados) 

colocando o método construtor
gets/sets 
métodos de validação (veja os exemplos presentes no Exemplo #01). 


Crie a classe controller. 

CtrlPrograma e coloque nela o método main. 

Nesse método instancie quatro objetos Empresa e coloque-os em um objeto List. 

Ordene esses objetos pelo nome e liste-os na tela.

Depois ordene esses objetos pelo número de Empregados e liste-os. 

Depois disso, recupere o objeto Class associado à classe e liste os atributos e métodos presentes na classe.